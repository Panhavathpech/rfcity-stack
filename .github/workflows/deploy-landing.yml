name: Deploy Landing

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-upload:
    name: Build Vite site and sync to A2 Hosting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build production bundle
        env:
          VITE_CONTACT_API: ${{ secrets.VITE_CONTACT_API }}
          VITE_SHOW_ADMIN_LINK: ${{ secrets.VITE_SHOW_ADMIN_LINK }}
          VITE_ADMIN_PORTAL_URL: ${{ secrets.VITE_ADMIN_PORTAL_URL }}
        run: npm run build

      - name: Upload dist/ to A2 Hosting over FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.RFCITY_FTP_HOST }}
          username: ${{ secrets.RFCITY_FTP_USER }}
          password: ${{ secrets.RFCITY_FTP_PASS }}
          port: ${{ secrets.RFCITY_FTP_PORT }}
          protocol: ftps
          server-dir: /public_html/
          local-dir: dist/
          state-name: .ftp-deploy-sync-state-rfcity.json
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**

